version: '3.8'

services:
  recruitment-app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./db:/app/db
      - ./backend/uploads:/app/backend/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped
